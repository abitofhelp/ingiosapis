version: v1
name: buf.build/abitofhelp/ingiosapis
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  ignore:
    - google/type/datetime.proto
# The deps field is the way to express dependencies between BSR modules.
deps: [ ]